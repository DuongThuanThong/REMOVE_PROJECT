package com.uth.fms.common.enums;

public enum QcFailType {
    WORKER_ERROR,    // Lỗi do tay nghề thợ (cắt sai, lắp sai)
    MATERIAL_ERROR   // Lỗi do nguyên vật liệu (gỗ cong, nứt, phụ kiện hỏng)
}
